import styled from 'styled-components'
import { color } from 'shared/utils/styles'

export const Avatar = styled.div`
  margin: 0 auto;
  width: 150px;
  border-radius: 50%;
  border: 4px solid ${color.champagne};
  overflow: hidden;
`
